import 'package:flutter/material.dart';

class AppColors {
  // Cores das casas
  static const Color gryffindor = Color(0xFF740001);
  static const Color slytherin = Color(0xFF1A472A);
  static const Color hufflepuff = Color(0xFFD3A625);
  static const Color ravenclaw = Color(0xFF0E1A40);

  // Cores de fundo
  static const Color primaryColor = Color(0xFF0D0D0D);

  static const Color white = Color(0xFFF2F2F2);
}
